import React from 'react'

const PageTitle = ({title}:any) => {
  return (
    <div className=''>
      <h1 className="text-3xl font-bold py-4 px-10">{title}</h1>
    </div>
  )
}

export default PageTitle
